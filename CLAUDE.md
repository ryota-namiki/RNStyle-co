# RNstyle co.

- 仮想組織: `/company` → `.company/`（秘書が窓口）
- 経理・freee 操作: `keiri` サブエージェント（`.claude/agents/keiri.md`）に委譲
- 長文手順書: `docs/runbooks/`（必要時のみ Read）

## 出力

挨拶・前置き・結びは不要。結果と要点のみ、簡潔な日本語（体言止め・箇条書き）。敬語不要。最小トークンで返す。
